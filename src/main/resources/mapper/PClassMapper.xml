<?xml version="1.0" encoding="UTF-8"?>
<!--mapper的xml的头文件-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wyl.dao.PClassDao">

    <select id="getAllClass" resultType="com.wyl.pojo.PClass">
        select * from pclass
--         接着写一下班级的service层
    </select>
    <select id="GetClassId" resultType="com.wyl.pojo.PClass">
        select * from pclass where classid=#{classid}
    </select>
</mapper>
